# The application is live-only. Demo records are not bundled into production.
VITE_API_MODE=live

# Use the reverse-proxied path in production. For local development this may instead be
# http://localhost:5000/api/v1 (matching the ASP.NET launch profile).
VITE_API_BASE_URL=/api/v1
